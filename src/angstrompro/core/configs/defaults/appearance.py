DEFAULTS = {
    "theme": "dark",          # "dark" | "light" | "auto" (follows OS)
    "font_family": "",        # empty = AngstromPro cross-platform sans-serif
    "font_size": 10,
    "custom_icon_dir": "",    # extra directory for user-supplied icons; empty = bundled only
}
