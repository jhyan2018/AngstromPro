DEFAULTS = {
    "theme": "dark",          # "dark" | "light" | "auto" (follows OS)
    "accent_color": "",       # hex string e.g. "#4fc3f7", empty = qdarktheme default
    "font_family": "",        # empty = system default
    "font_size": 12,
    "icon_size": 20,          # toolbar/button icon size in px
    "custom_icon_dir": "",    # extra directory for user-supplied icons; empty = bundled only
}
